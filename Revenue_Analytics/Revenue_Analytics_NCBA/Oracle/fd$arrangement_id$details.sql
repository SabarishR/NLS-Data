CREATE TABLE  fd$arrangement_id$details (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
  CURRENCY varchar(500) NOT NULL,
  BAND_LIMIT varchar(500) NOT NULL,
  START_DATE date NOT NULL,
  END_DATE date NOT NULL,
  TOTAL_FD CLOB NOT NULL,
  FD_NORMAL CLOB NOT NULL,
  FD_REVIEW CLOB NOT NULL,
  CREATED_BY varchar(200) DEFAULT NULL,
  CREATED_ON TIMESTAMP DEFAULT NULL,
  UPDATED_BY varchar(200) DEFAULT NULL,
  UPDATED_ON TIMESTAMP DEFAULT NULL);
  
ALTER TABLE fd$arrangement_id$details ADD CONSTRAINT fd$arrangement_id$details_PK PRIMARY KEY ( ID ) ;